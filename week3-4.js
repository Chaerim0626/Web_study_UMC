function multiply10(num) {
    const res = num*10;
    return res;
} //함수선언

const data = multiply10(10); //함수 호출

const multiply10 = (num) => {
    const res = num*10;
    return res;
} //화살표 함수

const multiply10 = num => num * 10; 